name: remoteopenfast
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.11
  - dash
  - dash-bootstrap-components
  - pandas
  - plotly
  - pyyaml
  - numpy
  - pip
  - openfast-io
  - requests
  - tqdm
  - scipy>=1.9.0
  - psutil>=5.9.0  # Required for dash.testing
  - pip:
    - dash-extensions
